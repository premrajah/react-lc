import React from "react";
import { Pages } from "@material-ui/icons";

const PageHeader = (git ) => {
    return (

        <>
            PAge header
        </>
    );
};

export default PageHeader;
