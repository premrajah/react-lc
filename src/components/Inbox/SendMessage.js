import React, {useState} from "react";
import {Card, CardActions, CardContent, FormControl, InputLabel, MenuItem, Select} from "@material-ui/core";
import Typography from "@material-ui/core/Typography";

const SendMessage = () => {
    return (
        <>
            <div>Testing</div>
        </>
    );
};

export default SendMessage;
